---
import BaseCard from './BaseCard.astro';

const { post } = Astro.props;
---

<BaseCard
  title={post.title}
  description={post.description}
  date={post.date}
  url={`/posts/${post.slug}`}
  image={post.image}
  tags={post.tags}
/>
